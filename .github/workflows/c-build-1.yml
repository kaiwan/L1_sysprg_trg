name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: build cpudtl_tstsvr
      run: cd net_sockets/inet_domain/cpudtl_tstsvr/ ; make clean ; make ; ls -lt
 
